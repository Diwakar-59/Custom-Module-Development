# node.type.movie.yml
langcode: en
status: true
dependencies:
  enforced:
    module:
      - db_module # This is the name of the module we're using for this example
name: 'Events'
type: events
description: 'Content type that can be used to add <em>Events</em>'
help: ''
new_revision: false
preview_mode: 1
display_submitted: true
